// Extracellular potential
objref a
objref f1
proc insert_electracellular() { strdef filename
     filename = $s1
     f1 = new File()
     f1.ropen(filename)
     a = new Vector()
     a.scanf(f1)
     
     forall {
     	    i = int(n3d()/2)
	    e_extracellular(0.5) = a.x[i]
	    }
}

     
