// Extracellular potential
proc insert_extracellular() {local inte
	inte = $1
	insert_extracellular_soma(inte)
	insert_extracellular_axon(inte)
	insert_extracellular_dendrite(inte)
}

proc insert_electracellular() { local filename
     filename = $s1
     forall {
     	    i = int(n3d()/2)
	    e_extracellular(0.5) = V_nn(x3d(i), y3d(i), z3d(i), filename)
	    }
}

proc V_nn() {local x, y, z, filename
     x = $1
     y = $2
     z = $3
     filename = $s4
     
